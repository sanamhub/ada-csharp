<Project>

  <!-- Shared settings for every project. See ADA_WRAPPER_PLAN.md §1.4. -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <LangVersion>latest</LangVersion>
    <Nullable>enable</Nullable>
    <ImplicitUsings>enable</ImplicitUsings>
    <InvariantGlobalization>true</InvariantGlobalization>

    <!-- Org §5: lint errors block merge. Enforced by the compiler, not by review. -->
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
    <EnforceCodeStyleInBuild>true</EnforceCodeStyleInBuild>
    <AnalysisLevel>latest-all</AnalysisLevel>

    <!-- Deterministic, source-linked builds (org §13). -->
    <Deterministic>true</Deterministic>
    <ContinuousIntegrationBuild Condition="'$(CI)' == 'true'">true</ContinuousIntegrationBuild>
    <PublishRepositoryUrl>true</PublishRepositoryUrl>
    <EmbedUntrackedSources>true</EmbedUntrackedSources>

    <ManagePackageVersionsCentrally>true</ManagePackageVersionsCentrally>

    <!-- Fail restore on a known advisory rather than warn. Transitive too, since that is where
         they usually hide. NuGet.Config pins the audit source. -->
    <NuGetAudit>true</NuGetAudit>
    <NuGetAuditMode>all</NuGetAuditMode>
    <NuGetAuditLevel>moderate</NuGetAuditLevel>
    <WarningsNotAsErrors>NU1901;NU1902;NU1903;NU1904</WarningsNotAsErrors>
  </PropertyGroup>

  <PropertyGroup Label="Repository">
    <Product>Ada.Url</Product>
    <Company>Ada.Url contributors</Company>
    <Copyright>Copyright (c) Ada.Url contributors</Copyright>
    <RepositoryType>git</RepositoryType>
    <RepositoryUrl>https://github.com/sanamhub/ada-csharp</RepositoryUrl>
    <AdaUrlUpstreamTag>v4.0.0</AdaUrlUpstreamTag>
  </PropertyGroup>

</Project>
